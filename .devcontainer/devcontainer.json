{
  "name": "Pathfinder",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postStartCommand": "if [ -f requirements.txt ]; then pip install -r requirements.txt; fi",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}